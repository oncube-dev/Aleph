# Aleph Messenger

Полнофункциональное десктопное приложение-мессенджер на Python с использованием PyQt5.

## Особенности

- **Простая аутентификация** - вход только по ID пользователя без паролей
- **Текстовые сообщения** - отправка и получение сообщений в реальном времени
- **Голосовые звонки** - поддержка аудио-звонков через UDP
- **Онлайн-статус** - индикаторы онлайн/офлайн для всех пользователей
- **Локальная база данных** - SQLite для хранения сообщений и пользователей
- **Современный интерфейс** - красивый и интуитивный UI на PyQt5

## Архитектура

```
Aleph/
├── main.py              # Главный файл приложения
├── server.py            # Сервер для тестирования
├── config.py            # Конфигурация приложения
├── database.py          # Работа с базой данных
├── network_manager.py   # Сетевое взаимодействие
├── audio_manager.py     # Обработка аудио
├── auth_window.py       # Окно аутентификации
├── main_window.py       # Главное окно мессенджера
├── requirements.txt     # Зависимости
└── README.md           # Документация
```

## Установка

### Требования

- Python 3.7+
- PyQt5
- PyAudio
- SQLite3

### Установка зависимостей

```bash
pip install -r requirements.txt
```

### Для Windows (PyAudio)

Если возникают проблемы с PyAudio на Windows:

```bash
pip install pipwin
pipwin install pyaudio
```

## Запуск

### 1. Запуск сервера (в отдельном терминале)

```bash
python server.py
```

Сервер запустится на `127.0.0.1:5000`

### 2. Запуск клиента

```bash
python main.py
```

## Использование

### Первый запуск

1. При первом запуске откроется окно аутентификации
2. Введите уникальный ID пользователя (например, "user1")
3. Если пользователь не существует, система предложит создать профиль
4. После входа откроется главное окно мессенджера

### Тестовые пользователи

По умолчанию создаются следующие пользователи:
- `user1`
- `user2` 
- `user3`
- `admin`
- `test`

### Основные функции

#### Чат
- Выберите контакт из списка слева
- Введите сообщение в поле ввода
- Нажмите "Отправить" или Enter

#### Звонки
- Нажмите кнопку 📞 рядом с контактом
- Подтвердите начало звонка
- Для завершения нажмите OK в диалоге

#### Добавление контактов
- Нажмите "+ Добавить контакт"
- Введите ID существующего пользователя

## Конфигурация

Основные настройки в файле `config.py`:

```python
# Сетевые настройки
HOST = "127.0.0.1"
PORT = 5000

# Аудио настройки
AUDIO_SAMPLE_RATE = 44100
AUDIO_CHUNK_SIZE = 1024

# Интервалы
HEARTBEAT_INTERVAL = 30  # секунды
ONLINE_TIMEOUT = 60      # секунды
```

## Структура базы данных

### Таблица `users`
- `id` - уникальный идентификатор
- `user_id` - ID пользователя
- `display_name` - отображаемое имя
- `is_online` - онлайн-статус
- `last_seen` - время последней активности
- `created_at` - время создания

### Таблица `messages`
- `id` - уникальный идентификатор
- `sender_id` - ID отправителя
- `receiver_id` - ID получателя
- `message_text` - текст сообщения
- `timestamp` - время отправки
- `is_read` - статус прочтения

### Таблица `contacts`
- `id` - уникальный идентификатор
- `user_id` - ID пользователя
- `contact_id` - ID контакта
- `created_at` - время добавления

## Сетевое взаимодействие

### Протокол сообщений

Все сообщения передаются в формате JSON:

```json
{
    "type": "message_type",
    "sender_id": "user123",
    "receiver_id": "user456",
    "message_text": "Привет!",
    "timestamp": 1234567890
}
```

### Типы сообщений

- `message` - текстовое сообщение
- `status_update` - обновление статуса
- `heartbeat` - проверка активности
- `call_request` - запрос на звонок
- `call_response` - ответ на звонок
- `call_end` - завершение звонка
- `user_list_request` - запрос списка пользователей
- `user_list_response` - ответ со списком пользователей

## Аудио система

### Технологии
- **PyAudio** - для записи и воспроизведения
- **UDP сокеты** - для передачи аудио данных
- **Потоки** - для параллельной обработки

### Особенности
- Автоматическое определение аудио устройств
- Настраиваемые параметры качества
- Буферизация для плавного воспроизведения

## Разработка

### Добавление новых функций

1. **Новый тип сообщения**: добавьте обработчик в `NetworkManager`
2. **Новый UI элемент**: создайте виджет в соответствующем модуле
3. **Новая таблица БД**: добавьте схему в `Database.init_database()`

### Тестирование

```bash
# Запуск сервера
python server.py

# В другом терминале - клиент 1
python main.py

# В третьем терминале - клиент 2  
python main.py
```

## Устранение неполадок

### Проблемы с PyAudio

**Windows:**
```bash
pip install pipwin
pipwin install pyaudio
```

**Linux:**
```bash
sudo apt-get install python3-pyaudio
sudo apt-get install portaudio19-dev
pip install pyaudio
```

**macOS:**
```bash
brew install portaudio
pip install pyaudio
```

### Проблемы с сетью

1. Проверьте, что сервер запущен
2. Убедитесь, что порт 5000 не занят
3. Проверьте настройки файрвола

### Проблемы с базой данных

1. Удалите файл `messenger.db`
2. Перезапустите приложение
3. База данных будет создана заново

## Лицензия

Этот проект создан в образовательных целях.

## Автор

Создано с помощью AI Assistant

## Поддержка

При возникновении проблем:
1. Проверьте логи в консоли
2. Убедитесь, что все зависимости установлены
3. Проверьте настройки в `config.py`
